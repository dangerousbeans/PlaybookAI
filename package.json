{
  "name": "playbookai",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:css": "sass --style compressed custom-bootstrap.scss css/bootstrap-custom.css",
    "watch:css": "sass --watch custom-bootstrap.scss:css/bootstrap-custom.css",
    "predeploy": "npm run build:css",
    "deploy": "gh-pages -d ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^5.3.4",
    "sass": "^1.86.2"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
